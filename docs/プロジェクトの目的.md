# WinForm
# DBを選択する
# 接続文字列を登録する
# 接続文字列を保存する
# テーブル一覧を取得する
## テーブル一覧を画面に表示する

# テーブルのDDLを取得する
## テーブルのDDLを取得してテストデータを作る
### 作ったデータをCSVで保存する
### 作ったデータをtempテーブルを作ってそこに保存する

# Null許容のカラムにはNullと空白とデータ有の３パターンを必ず用意する
# カラムが文字列だった場合、指定のCSVファイルからランダムに取得する
# カラムが数字だった場合、０と最大値を必ず用意する
# 最低件数は自動で算出、最大件数は指定可能
# 条件をパターンとして登録できる
# MDファイルに実行前のデータを出す
# MDファイルに実行後のデータを出す
# MDファイルにデータの差分を出す
# テーブルの外部キーを取得する
# テーブルのデータを０件にする
# テーブルデータをCSVに保存してクリア
# テーブルデータを日付付きの添付テーブルに保存
# テーブルデータをCSVからもとに戻す
# テーブルデータを日付付きの添付テーブルから戻す
# テーブルのカラムのデータ型を取得する
# テーブルインデックスを取得する
# テーブルのデータ作成件数を指定する
# テーブルのカラムとそのカラムのデータ型に応じたサンプルデータを作成する
# テーブルのサンプルデータをデータ自動生成する
# テーブルのサンプルデータをサンプルデータ保管庫から取得する
# テーブルのカラムを指定してデータの規則性を指定する
# データの組み合わせ種類を指定する(Aのテーブルにはデータがある,Bのテーブルにはデータが無い)
# データの組み合わせ種類を指定する(AのテーブルにはAデータがある,BのテーブルにはBのデータがある)
# データをCSVに取得して保管する
# データをCSVから投入する
# データを